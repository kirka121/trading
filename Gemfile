# frozen_string_literal: true

source 'https://rubygems.org'

ruby '4.0.2'

gem 'dotenv', '~> 3.1'
gem 'csv',    '~> 3.3'

# TradingView MCP server (lib/mcp/tradingview/) — drives TradingView Desktop
# via the Chrome DevTools Protocol and exposes the resulting tools over the
# Model Context Protocol stdio transport.
gem 'mcp',              '~> 0.14'
gem 'websocket-driver', '~> 0.7'
